{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @chatv2/web exec vite build",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
