{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @chatv2/shared build && cd apps/web && npx vite build --outDir ../../dist",
  "outputDirectory": "dist",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
