{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "cd apps/web && npx vite build --outDir ../../dist",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
