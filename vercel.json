{
  "buildCommand": "pnpm install --frozen-lockfile && cd apps/web && npx vite build --outDir ../../dist",
  "outputDirectory": "dist",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
